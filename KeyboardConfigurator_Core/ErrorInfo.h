#pragma once
#include <string>
#include <vector>
#include <filesystem>
#include <Windows.h>
/*
enum
{
  ERROR_INFO,
  ERROR_WARN,
  ERROR_ERROR
};

void StackError(const BYTE, const UINT, const std::wstring);
bool ExportError(const std::filesystem::path);
*/